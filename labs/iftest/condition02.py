#!/usr/bin/env python3
"""
if else 
"""
def main():
    """runtime"""

    #asking user for hostname
    hostname = input("What value should we set for hostname?")
    
    # checking if hostname is equals to MTG
    if hostname == "MTG":

        #if true then:
        print("The hostname was found to be MTG")

if __name__ == "__main__":
    main()
